const browserVersion = "Chrome"

function getBrowserVersion ()
{

if (browserVersion== "Chrome")
{
    //var browserVersion = "Edge" //prints undefinded
    let browserVersion = "firefox"

    console.log("Browser version using let ", browserVersion);//prints firefox
}

console.log("Browser version using let ", browserVersion);
}
getBrowserVersion()